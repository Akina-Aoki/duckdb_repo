- After creating the `app.py`, `dockerfile`, and `docker-compose.yml` files, you can build and run the Docker container using Docker Compose.

- In the terminal, run `docker compose up -d` to build and run the container in detached mode.